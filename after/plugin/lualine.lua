require("lualine").setup({
    options = {
        theme = "palenight",
        refresh = {
            statusline = 100,
            tabline = 100,
            winbar = 100,
        },

    },
})
require('sunset')

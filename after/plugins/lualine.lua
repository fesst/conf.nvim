require('lualine').setup({
    options = {
        theme = "rose-pine-alt", ---@usage 'rose-pine' | 'rose-pine-alt'
        refresh = {
            statusline = 100,
            tabline = 100,
            winbar = 100,
        }
    }
})
